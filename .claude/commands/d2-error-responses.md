Show me how to build a tool error response layer for an MCP server that always returns structured errors with isError, errorCategory, isRetryable, and a context string — and never returns a generic operation failed message. In in `d2_tools/cert/error_responses/`. Give me a concrete example for an interpretability tool that might fail due to missing activations, model not loaded, or out-of-memory.