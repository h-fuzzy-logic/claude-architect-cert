Show me how to implement programmatic hooks in the Claude Agent SDK for deterministic enforcement in `d1_agentic/cert/hook_enforcement/`. For example, a hook that blocks any tool call involving a sensitive path, and an escalation pattern that triggers human review based on structured criteria rather than sentiment.

