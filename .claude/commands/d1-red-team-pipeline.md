Help me build a Petri-style multi-turn red-teaming system using the Claude Agent SDK instead of OpenRouter in `d1_agentic/arena/red_team_pipeline/`. I want a coordinator that decomposes an investigation, subagents that run probe conversations, and a judge subagent that scores results. Focus on proper context isolation and error propagation between agents.

