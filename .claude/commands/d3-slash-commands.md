Help me build custom slash commands for my ARENA research workflow in Claude Code: /run-interp-experiment that scaffolds a new interpretability experiment, /generate-eval-dataset that kicks off the eval pipeline, and /probe-model that runs a red-teaming session in `d3_claude_code/cert/slash_commands/`. Show me the skills file structure and how plan mode should be used before each.